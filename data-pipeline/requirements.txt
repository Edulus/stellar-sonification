# Core (required for the default offline build: NIST fetch + seed + templates)
requests>=2.28
numpy>=1.24

# Survey / synthetic-spectra paths (optional — only needed when running
# build-bright-stars.py --survey or build-templates.py --phoenix, which read FITS).
# A default run does not import these.
astropy>=5.0
scipy>=1.10
tqdm>=4.0
